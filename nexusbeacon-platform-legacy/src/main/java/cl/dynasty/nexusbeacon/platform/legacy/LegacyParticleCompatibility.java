package cl.dynasty.nexusbeacon.platform.legacy;

public enum LegacyParticleCompatibility {
    EXACT,
    LEGACY_ALIAS,
    LEGACY_EFFECT_EQUIVALENT,
    VISUAL_APPROXIMATION,
    UNSUPPORTED,
    INVALID
}
